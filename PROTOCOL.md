# 通信プロトコル仕様

`cugo_v4.5_ros2_control` と `cugo_v4.5_ros2_motorcontroller` 間の通信仕様です。


## パケット構成

送受信ともにヘッダ 8 byte + ボディ 64 byte = 合計 72 byte の固定長パケットを使用します。
パケット全体は COBS (Consistent Overhead Byte Stuffing) でエンコードして転送します。


## ヘッダフォーマット (8 byte)

| オフセット | サイズ | データ型 | 内容                                           |
| ---------- | ------ | -------- | ---------------------------------------------- |
| 0          | 2 byte | uint16_t | [プロダクトID](#プロダクトid)                  |
| 2          | 2 byte | uint16_t | [ロボットID](#ロボットid)                      |
| 4          | 2 byte | uint16_t | 電文長 (72 固定)                               |
| 6          | 2 byte | uint16_t | チェックサム (IP チェックサム、ボディのみ対象) |


## ボディフォーマット (64 byte)

### PC → マイコン (速度指令・IO 制御)

| オフセット | サイズ   | データ型 | 内容                                                                |
| ---------- | -------- | -------- | ------------------------------------------------------------------- |
| 0          | 2 byte   | int16_t  | 目標 X 方向速度 (値 × 0.001 m/s)                                    |
| 2          | 2 byte   | int16_t  | 目標 Y 方向速度 (値 × 0.001 m/s、常に 0)                            |
| 4          | 2 byte   | int16_t  | 目標旋回速度 (値 × 0.001 rad/s)                                     |
| 6          | 1 byte   | uint8_t  | [enable_7_14](#enable_7_14-ビット定義) — body[7–14] 有効ビットマスク|
| 7          | 1 byte   | uint8_t  | 走行モード切替 (0x80=RC / 0x81=CMD) ※enable bit0 が 1 のとき有効   |
| 8          | 1 byte   | uint8_t  | 緊急減速トリガ (非零で発動) ※enable bit1 が 1 のとき有効           |
| 9          | 1 byte   | uint8_t  | クリアするコントローラエラービット ※enable bit2 が 1 のとき有効    |
| 10         | 1 byte   | uint8_t  | クリアするモータドライバエラービット ※enable bit3 が 1 のとき有効  |
| 11         | 1 byte   | uint8_t  | ヘッドライト制御値 ※enable bit4 が 1 のとき有効                    |
| 12         | 1 byte   | uint8_t  | タワーライト制御値 ※enable bit5 が 1 のとき有効                    |
| 13         | 1 byte   | uint8_t  | バンパー設定値 ※enable bit6 が 1 のとき有効                        |
| 14         | 1 byte   | uint8_t  | ブレーキ設定値 ※enable bit7 が 1 のとき有効                        |
| 15–59      | 45 byte  | —        | 予約 (0x00)                                                         |
| 60         | 2 byte   | uint16_t | 通信相手として期待する[プロダクトID](#プロダクトid) (ヘッダと同値)  |
| 62         | 2 byte   | uint16_t | 通信相手として期待する[ロボットID](#ロボットid) (ヘッダと同値)      |

### マイコン → PC (現在速度・センサデータ)

| オフセット | サイズ  | データ型 | 内容                                               |
| ---------- | ------- | -------- | -------------------------------------------------- |
| 0          | 2 byte  | int16_t  | 現在 X 方向速度 (値 × 0.001 m/s)                   |
| 2          | 2 byte  | int16_t  | 現在 Y 方向速度 (値 × 0.001 m/s、常に 0)           |
| 4          | 2 byte  | int16_t  | 現在旋回速度 (値 × 0.001 rad/s)                    |
| 6          | 1 byte  | uint8_t  | コントローラステータス                             |
| 7          | 1 byte  | uint8_t  | コントローラエラービット                           |
| 8          | 1 byte  | uint8_t  | モータドライバエラービット                         |
| 9          | 2 byte  | uint16_t | ドライバ電圧 raw (実電圧 = raw × 0.1 V)            |
| 11         | 1 byte  | uint8_t  | ヘッドライト状態                                   |
| 12         | 1 byte  | uint8_t  | タワーライト状態                                   |
| 13         | 1 byte  | uint8_t  | 外部 4bit デジタル入力                             |
| 14         | 4 byte  | uint32_t | エンコーダカウント (モータ 0)                      |
| 18         | 4 byte  | uint32_t | エンコーダカウント (モータ 1)                      |
| 22         | 4 byte  | uint32_t | エンコーダカウント (モータ 2)                      |
| 26         | 4 byte  | uint32_t | エンコーダカウント (モータ 3)                      |
| 30         | 2 byte  | uint16_t | モータドライバ温度 [℃] (モータ 0)                  |
| 32         | 2 byte  | uint16_t | モータドライバ温度 [℃] (モータ 1)                  |
| 34         | 2 byte  | uint16_t | モータドライバ温度 [℃] (モータ 2)                  |
| 36         | 2 byte  | uint16_t | モータドライバ温度 [℃] (モータ 3)                  |
| 38         | 2 byte  | uint16_t | モータドライバエラーコード (モータ 0)              |
| 40         | 2 byte  | uint16_t | モータドライバエラーコード (モータ 1)              |
| 42         | 2 byte  | uint16_t | モータドライバエラーコード (モータ 2)              |
| 44         | 2 byte  | uint16_t | モータドライバエラーコード (モータ 3)              |
| 46         | 1 byte  | uint8_t  | バンパー設定値 (フラッシュ保存値の読み出し)        |
| 47         | 1 byte  | uint8_t  | ブレーキ設定値 (フラッシュ保存値の読み出し)        |
| 48–59      | 12 byte | —        | 予約 (0x00)                                        |
| 60         | 2 byte  | uint16_t | 自身の[プロダクトID](#プロダクトid) (ヘッダと同値) |
| 62         | 2 byte  | uint16_t | 自身の[ロボットID](#ロボットid) (ヘッダと同値)     |


## enable_7_14 ビット定義

body[6] の各ビットが `1` の場合のみ、対応する body[7–14] のフィールドが車両コントローラへ反映されます。
速度指令と IO 制御指令は同一パケットに含まれますが、IO 制御は enable_7_14 で独立して制御できます。

| ビット | マスク | 対応フィールド | 内容                           |
| ------ | ------ | -------------- | ------------------------------ |
| bit0   | 0x01   | body[7]        | 走行モード切替                 |
| bit1   | 0x02   | body[8]        | 緊急減速                       |
| bit2   | 0x04   | body[9]        | コントローラエラー解除         |
| bit3   | 0x08   | body[10]       | モータドライバエラー解除       |
| bit4   | 0x10   | body[11]       | ヘッドライト制御               |
| bit5   | 0x20   | body[12]       | タワーライト制御               |
| bit6   | 0x40   | body[13]       | バンパー設定書き込み           |
| bit7   | 0x80   | body[14]       | ブレーキ設定書き込み           |


## 通信シーケンス

### ハンドシェイク

通信開始時に PC とマイコン双方が正しい相手と接続できているかを確認します。

1. PC がイニシャル要求パケット(ボディの先頭 60 byte が全て 0x00、末尾 4 byte にプロダクトID / ロボットID)を送信します
2. マイコンはイニシャル要求を受信すると CMD モードへ移行し、イニシャル応答を返します
3. ハンドシェイク完了後、通常の速度指令・現在速度の送受信を開始します

ハンドシェイクが完了するまでロボットは走行を開始しません。

### フェイルセーフ

マイコン側は 100 ms ごとに通信状態を監視します。
5 回連続(500 ms)パケットを受信できなかった場合、自動的に速度 0 に設定します。
通信が回復した際はフェイルセーフ状態を解除し、再度ハンドシェイクを行います。

## 識別子

### プロダクトID

CuboRex が販売する製品ごとに設定される識別子です。
プロトコル互換性は `product_id / 10000` の商(プロトコル識別子)で判断します。
プロトコル識別子が一致しない機器を接続した場合、通信は失敗します。

| 製品      | プロダクトID | プロトコル識別子 |
| --------- | ------------ | ---------------- |
| CuGo V4.5 | 10000        | 1                |

### ロボットID

複数台のロボットを識別するための値です。
現バージョンではロボットIDによる処理の切り替えは実装していません。
